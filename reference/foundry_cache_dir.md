# Resolve the foundryR embedding cache directory

Resolve the foundryR embedding cache directory

## Usage

``` r
foundry_cache_dir(cache_dir = NULL)
```

## Arguments

- cache_dir:

  Character or NULL. Explicit cache directory.

## Value

An absolute path to the cache directory.
