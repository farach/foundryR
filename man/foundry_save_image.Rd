% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/image.R
\name{foundry_save_image}
\alias{foundry_save_image}
\title{Save Generated Image to File}
\usage{
foundry_save_image(image_result, path, index = 1)
}
\arguments{
\item{image_result}{A tibble returned by \code{foundry_image()}.}

\item{path}{Character. The file path where the image should be saved.
Should include the file extension (e.g., ".png").}

\item{index}{Integer. Which image to save if multiple were generated (1-based).
Default: 1 (first image).}
}
\value{
Invisibly returns the path to the saved file.
}
\description{
Download and save a generated image from \code{foundry_image()} to a local file.
Works with both URL and base64-encoded image results.
}
\details{
This function handles both URL and base64-encoded images automatically.
Base64-encoded image data is the normal GPT-image path; it is decoded and
written to \code{path}. URL handling is kept for legacy deployments whose
temporary image URLs expire.

\strong{Note}: If a legacy deployment returns a URL, use this function to save the
image locally before the temporary URL expires.
}
\examples{
# Save a one-pixel PNG without calling Azure.
if (requireNamespace("base64enc", quietly = TRUE)) {
  local({
    image <- tibble::tibble(
      url = NA_character_,
      b64_json = paste0(
        "iVBORw0KGgoAAAANSUhEUgAAAAEAAAABCAQAAAC1HAwCAAAAC0lEQVR42mP8",
        "/x8AAwMCAO+ip1sAAAAASUVORK5CYII="
      )
    )
    path <- tempfile(fileext = ".png")
    on.exit(unlink(path))
    foundry_save_image(image, path)
    file.exists(path)
  })
}

\dontrun{
# Requires a configured Azure image endpoint and credentials,
# plus an image-generation deployment.
local({
  paths <- replicate(3, tempfile(fileext = ".png"))
  on.exit(unlink(paths))

  result <- foundry_image(
    "Colorful abstract art",
    model = "gpt-image-2",
    n = 3
  )
  if (requireNamespace("base64enc", quietly = TRUE)) {
    foundry_save_image(result, paths[1], index = 1)
    foundry_save_image(result, paths[2], index = 2)
    foundry_save_image(result, paths[3], index = 3)
  }
})
}
}
