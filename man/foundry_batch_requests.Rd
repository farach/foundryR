% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/batches.R
\name{foundry_batch_requests}
\alias{foundry_batch_requests}
\title{Write JSONL requests for the Batch API}
\usage{
foundry_batch_requests(
  data,
  input,
  path,
  model,
  endpoint = "/v1/responses",
  custom_id = NULL,
  body = list(),
  schema = NULL,
  schema_name = "ExtractedData",
  strict = TRUE,
  instructions = NULL,
  body_columns = NULL,
  overwrite = FALSE
)
}
\arguments{
\item{data}{Data frame containing input rows.}

\item{input}{Character. Name of the column containing prompt/input text.}

\item{path}{Character. Path to write the JSONL file.}

\item{model}{Character. Model deployment name to include in each request.}

\item{endpoint}{Character. Batch endpoint path. Defaults to \code{"/v1/responses"}.}

\item{custom_id}{Character. Optional column name for custom IDs. If omitted,
IDs are generated as \code{row-1}, \code{row-2}, and so on.}

\item{body}{List. Additional request body fields added to each request.}

\item{schema}{List. Optional JSON Schema for structured Responses API output.}

\item{schema_name}{Character. Name for \code{schema} when supplied.}

\item{strict}{Logical. Whether structured output should be strict.}

\item{instructions}{Character. Optional instructions for Responses API
requests.}

\item{body_columns}{Character vector. Optional column names whose per-row
values should be added to each request body.}

\item{overwrite}{Logical. Whether to overwrite an existing file.}
}
\value{
A tibble with the JSONL path, request count, and endpoint.
}
\description{
Convert a data frame of prompts into a JSON Lines file that can be uploaded
with \code{foundry_file_upload(..., purpose = "batch")} and submitted with
\code{foundry_batch_create()}.
}
\examples{
jobs <- data.frame(text = c("Summarize this.", "Extract entities."))
path <- tempfile(fileext = ".jsonl")
foundry_batch_requests(jobs, input = "text", path = path, model = "gpt-5-nano")
}
