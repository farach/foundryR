% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/evals.R
\name{foundry_grader_label_model}
\alias{foundry_grader_label_model}
\title{Label-model grader}
\usage{
foundry_grader_label_model(name, model, input, labels, passing_labels)
}
\arguments{
\item{name}{Character. Grader name.}

\item{model}{Character. Deployment name of a model that supports structured
outputs.}

\item{input}{List. A list of items from \code{\link[=foundry_eval_item]{foundry_eval_item()}} (or a single
item), forming the grading prompt.}

\item{labels}{Character vector. The complete set of labels the model may
assign.}

\item{passing_labels}{Character vector. The labels that count as a pass. Must
be a subset of \code{labels}.}
}
\value{
A named list describing a \code{label_model} grader.
}
\description{
Use a model to assign one of a fixed set of labels to each row, then treat a
subset of those labels as passing. The model must support structured outputs.
}
\examples{
foundry_grader_label_model(
  name = "relevance-label",
  model = "gpt-5.5",
  input = list(
    foundry_eval_item("Is the answer relevant? {{sample.output_text}}")
  ),
  labels = c("relevant", "irrelevant"),
  passing_labels = "relevant"
)
}
