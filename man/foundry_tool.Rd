% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/responses.R
\name{foundry_tool}
\alias{foundry_tool}
\title{Define an R function as a Responses API tool}
\usage{
foundry_tool(fun, name = NULL, description, parameters)
}
\arguments{
\item{fun}{Function. The R function to run when the model calls the tool.}

\item{name}{Character. Tool name exposed to the model. If omitted and \code{fun}
is a named function object, the object name is used.}

\item{description}{Character. Short description of what the tool does.}

\item{parameters}{List. JSON Schema object describing function arguments.}
}
\value{
A \code{foundry_tool} object. It is a list containing the JSON tool schema
and the R function used by \code{foundry_agent()}.
}
\description{
Create a tool definition for \code{foundry_response()} or \code{foundry_agent()}. The
request sent to Azure uses the Responses API function-tool contract, while
the returned object also keeps the R function needed for local dispatch.
}
\examples{
get_weather <- function(location) {
  list(location = location, temperature = "70 F")
}

weather_tool <- foundry_tool(
  get_weather,
  description = "Get weather for a location",
  parameters = list(
    type = "object",
    properties = list(location = list(type = "string")),
    required = "location"
  )
)
}
