% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/auth.R
\name{foundry_set_token}
\alias{foundry_set_token}
\title{Set Microsoft Foundry Bearer Token}
\usage{
foundry_set_token(token, store = FALSE, scope = c("resource", "project"))
}
\arguments{
\item{token}{Character string containing a bearer token. Do not include the
\code{"Bearer "} prefix.}

\item{store}{Logical. If \code{TRUE}, stores the token in foundryR's
package-specific user configuration file. Tokens expire, so persistent
tokens are usually only useful for local testing.}

\item{scope}{Character. Endpoint family for the token: \code{"resource"} for
resource-level \verb{/openai/v1} and supported Content Safety operations, or
\code{"project"} for \verb{/api/projects/...} operations.}
}
\value{
Invisibly returns \code{TRUE} if the token was set successfully.
}
\description{
Set a Microsoft Entra ID bearer token for keyless authentication. API keys
remain supported, but Microsoft recommends keyless authentication for
production workloads.
}
\examples{
\dontshow{if (requireNamespace("withr", quietly = TRUE)) withAutoprint(\{ # examplesIf}
withr::with_envvar(c(AZURE_FOUNDRY_TOKEN = NA_character_), {
foundry_set_token("eyJ0eXAiOiJKV1QiLCJhbGciOi...")
})
\dontshow{\}) # examplesIf}
}
