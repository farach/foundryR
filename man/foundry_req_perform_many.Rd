% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils.R
\name{foundry_req_perform_many}
\alias{foundry_req_perform_many}
\title{Perform Many Requests}
\usage{
foundry_req_perform_many(reqs, progress = FALSE, max_active = 10)
}
\arguments{
\item{reqs}{A list of httr2 request objects.}

\item{progress}{Passed to \code{httr2::req_perform_parallel()}.}

\item{max_active}{Passed to \code{httr2::req_perform_parallel()}.}
}
\value{
A list of responses or error conditions, in the order of \code{reqs}.
}
\description{
Internal helper that performs a list of httr2 requests. By default it uses
\code{httr2::req_perform_parallel()} for speed. When the option
\code{foundryR.sequential_requests} is \code{TRUE}, the requests are performed one at a
time with \code{httr2::req_perform()} instead.
}
\details{
Parallel requests bypass httr2's mocking hook, so the sequential path is what
lets httptest2 record and replay documentation fixtures for batched calls such
as \code{foundry_embed()} and \code{foundry_extract()} (see
\code{inst/httptest2/start-vignette.R}). Both paths return a list, in request order,
whose elements are either an httr2 response or the error condition raised for
that request, mirroring \code{req_perform_parallel(on_error = "continue")}.
}
\keyword{internal}
