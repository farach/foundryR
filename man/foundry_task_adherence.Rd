% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/content-safety-preview.R
\name{foundry_task_adherence}
\alias{foundry_task_adherence}
\title{Check an agent transcript for task adherence}
\usage{
foundry_task_adherence(
  messages,
  tools = NULL,
  endpoint = NULL,
  api_key = NULL,
  api_version = "2025-09-15-preview"
)
}
\arguments{
\item{messages}{List. The conversation turns to analyze. Build each turn with
\code{\link[=foundry_agent_message]{foundry_agent_message()}}, or supply raw lists matching the Content Safety
schema.}

\item{tools}{List. Optional tool definitions available to the agent. Build
each with \code{\link[=foundry_agent_tool]{foundry_agent_tool()}}, or supply raw lists. Default \code{NULL}.}

\item{endpoint}{Character. Optional Content Safety endpoint.}

\item{api_key}{Character. Optional Content Safety key.}

\item{api_version}{Character. API version. Defaults to
\code{"2025-09-15-preview"}.}
}
\value{
A tibble with one row:
\describe{
\item{task_risk_detected}{Logical. \code{TRUE} when misaligned tool use was
detected.}
\item{details}{Character. Explanation of the detected risk, or \code{NA} when
none.}
\item{raw_response}{List. The parsed API response.}
}
}
\description{
Evaluate whether an agent's tool calls and responses stayed aligned with the
user's request using the Azure AI Content Safety task-adherence detector.
This flags agents that take unrequested or unsafe actions.
}
\section{Preview API}{

This operation is documented only in the Azure AI Content Safety Learn
quickstart and has no published OpenAPI specification. It requires the
\code{2025-09-15-preview} api-version and its contract may change.
}

\examples{
\dontrun{
foundry_task_adherence(
  tools = list(
    foundry_agent_tool("get_credit_card_limit", "Get the user's credit limit")
  ),
  messages = list(
    foundry_agent_message("Prompt", "User", "What is my limit?"),
    foundry_agent_message(
      "Completion", "Assistant", "Checking now",
      tool_calls = list(
        foundry_agent_tool_call("get_credit_card_limit", id = "call_001")
      )
    )
  )
)
}
}
