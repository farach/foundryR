% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/moderate.R
\name{foundry_moderate}
\alias{foundry_moderate}
\title{Moderate Text Content}
\usage{
foundry_moderate(
  text,
  categories = c("Hate", "Sexual", "SelfHarm", "Violence"),
  output_type = c("FourSeverityLevels", "EightSeverityLevels"),
  endpoint = NULL,
  api_key = NULL,
  api_version = "2024-09-01"
)
}
\arguments{
\item{text}{Character vector. The text(s) to analyze. Each text must be
10,000 characters or less.}

\item{categories}{Character vector. Categories to analyze. Must be a subset of
\code{c("Hate", "Sexual", "SelfHarm", "Violence")}. Default: all four categories.}

\item{output_type}{Character. Severity level granularity. One of
\code{"FourSeverityLevels"} (returns 0, 2, 4, 6) or \code{"EightSeverityLevels"}
(returns 0-7). Default: \code{"FourSeverityLevels"}.}

\item{endpoint}{Character. Optional endpoint URL override. If NULL, uses the
\code{AZURE_CONTENT_SAFETY_ENDPOINT} environment variable.}

\item{api_key}{Character. Optional API key override. If NULL, uses the
\code{AZURE_CONTENT_SAFETY_KEY} environment variable.}

\item{api_version}{Character. API version to use. Default: \code{"2024-09-01"}.}
}
\value{
A tibble with columns:
\describe{
\item{text}{Character. The input text (truncated to 50 chars if longer).}
\item{category}{Character. The harm category: "Hate", "Sexual", "SelfHarm", or "Violence".}
\item{severity}{Integer. Severity score. Range depends on \code{output_type}:
0-6 for FourSeverityLevels (values: 0, 2, 4, 6) or 0-7 for EightSeverityLevels.}
\item{label}{Character. Human-readable severity label: "safe", "low", "medium", or "high".}
}
}
\description{
Analyze text content for potentially harmful material using the Azure Content
Safety API. Returns severity scores for multiple harm categories including
hate speech, sexual content, self-harm, and violence.
}
\details{
The Azure Content Safety API analyzes text for four types of harmful content:

\itemize{
\item \strong{Hate}: Content that attacks or discriminates against individuals
or groups based on protected attributes.
\item \strong{Sexual}: Sexually explicit or adult content.
\item \strong{SelfHarm}: Content that promotes or describes self-harm behaviors.
\item \strong{Violence}: Content that describes or promotes violence.
}

\strong{Severity Labels:}
\itemize{
\item \strong{safe} (0): No harmful content detected.
\item \strong{low} (1-2): Mildly concerning content.
\item \strong{medium} (3-4): Moderately harmful content.
\item \strong{high} (5+): Severely harmful content.
}
}
\section{Authentication}{

You need an Azure Content Safety resource to use this function. Set up
credentials using either:
\itemize{
\item Environment variables: \code{AZURE_CONTENT_SAFETY_ENDPOINT} and \code{AZURE_CONTENT_SAFETY_KEY}
\item Helper functions: \code{foundry_set_content_safety_endpoint()} and \code{foundry_set_content_safety_key()}
}
}

\examples{
\dontrun{
# Analyze a single text
foundry_moderate("This is a friendly message.")

# Analyze multiple texts
texts <- c(
  "Hello, how are you today?",
  "This is another message to check."
)
results <- foundry_moderate(texts)

# Filter for specific categories
foundry_moderate("Some text", categories = c("Hate", "Violence"))

# Use finer-grained severity levels
foundry_moderate("Some text", output_type = "EightSeverityLevels")

# Check results
library(dplyr)
results \%>\%
  filter(severity > 0) \%>\%
  arrange(desc(severity))
}
}
