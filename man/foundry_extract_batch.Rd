% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/batches.R
\name{foundry_extract_batch}
\alias{foundry_extract_batch}
\title{Extract structured data with the Batch API}
\usage{
foundry_extract_batch(
  data,
  text_col,
  schema,
  model,
  wait = FALSE,
  path = tempfile(fileext = ".jsonl"),
  schema_name = "ExtractedData",
  strict = TRUE,
  instructions = NULL,
  completion_window = "24h",
  api_key = NULL,
  token = NULL,
  endpoint_url = NULL,
  api_version = NULL
)
}
\arguments{
\item{data}{Data frame containing input rows.}

\item{text_col}{Character. Name of the column containing input text.}

\item{schema}{List. JSON Schema object for structured extraction.}

\item{model}{Character. Model deployment name to include in each request.}

\item{wait}{Logical. Whether to block until the batch reaches a terminal
state and parse results.}

\item{path}{Character. Optional JSONL path. Defaults to a temporary file.}

\item{schema_name}{Character. Name for \code{schema} when supplied.}

\item{strict}{Logical. Whether structured output should be strict.}

\item{instructions}{Character. Optional instructions for Responses API
requests.}

\item{completion_window}{Character. Batch completion window, usually \code{"24h"}.}

\item{api_key}{Character. Optional API key override.}

\item{token}{Character. Optional bearer token override.}

\item{endpoint_url}{Character. Optional Foundry endpoint override.}

\item{api_version}{Character. Optional API version query value.}
}
\value{
A batch tibble when \code{wait = FALSE}, or parsed result rows when
\code{wait = TRUE}.
}
\description{
Prepare JSONL requests for structured extraction, upload them, and create a
batch. With \code{wait = TRUE}, waits for completion and returns parsed results
joined back to the input rows.
}
\examples{
\dontrun{
jobs <- data.frame(text = c("Great service.", "Slow support."))
schema <- foundry_schema(sentiment = schema_string())
foundry_extract_batch(jobs, text_col = "text", schema = schema, model = "gpt-4.1")
}
}
