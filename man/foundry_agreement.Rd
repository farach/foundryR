% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/validation.R
\name{foundry_agreement}
\alias{foundry_agreement}
\title{Compute agreement metrics for LLM annotation}
\usage{
foundry_agreement(data, estimate, truth)
}
\arguments{
\item{data}{Data frame containing estimates and truth.}

\item{estimate}{Character. Column name with model labels.}

\item{truth}{Character. Column name with reference labels.}
}
\value{
A tibble with one row per metric.
}
\description{
Compare model labels with human or gold-standard labels using common
publication-friendly metrics: accuracy, macro precision/recall/F1, Cohen's
kappa, and Krippendorff's alpha (using \pkg{irr} when installed, otherwise a
base-R nominal implementation).
}
\examples{
labels <- data.frame(
  model = c("yes", "no", "yes"),
  human = c("yes", "no", "no")
)
foundry_agreement(labels, estimate = "model", truth = "human")
}
