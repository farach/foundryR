% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/responses.R
\name{foundry_agent}
\alias{foundry_agent}
\title{Run a bounded Responses API tool-calling loop}
\usage{
foundry_agent(
  input,
  tools,
  model = NULL,
  instructions = NULL,
  max_iterations = 8L,
  store = TRUE,
  reasoning_effort = NULL,
  max_output_tokens = NULL,
  temperature = NULL,
  top_p = NULL,
  api_key = NULL,
  endpoint = NULL,
  ...
)
}
\arguments{
\item{input}{Character scalar or list. Initial user input for the response.}

\item{tools}{A \code{\link[=foundry_tool]{foundry_tool()}} object or list of \code{foundry_tool} objects.}

\item{model}{Character. The model deployment name. Defaults to the
\code{AZURE_FOUNDRY_MODEL} environment variable.}

\item{instructions}{Character. Optional system/developer instructions.}

\item{max_iterations}{Integer. Maximum number of model responses in the loop.}

\item{store}{Logical. Whether Responses API objects should be stored.
Defaults to \code{TRUE} because the loop uses \code{previous_response_id}.}

\item{reasoning_effort}{Character. Optional reasoning effort for reasoning
models.}

\item{max_output_tokens, temperature, top_p}{Optional generation controls passed
to \code{foundry_response()}.}

\item{api_key}{Character. Optional API key override.}

\item{endpoint}{Character. Optional endpoint override.}

\item{...}{Additional request body parameters passed to \code{foundry_response()}.}
}
\value{
A tibble with one row per model response. It includes the standard
\code{foundry_response()} columns plus \code{iteration}, \code{final}, and \code{tool_results}
list-columns for executed R tools.
}
\description{
\code{foundry_agent()} sends a prompt to the Responses API with user-defined R
tools, executes any returned function calls locally, sends matching
\code{function_call_output} items back to the service, and repeats until the model
returns a final answer or \code{max_iterations} is reached.
}
\examples{
\dontrun{
get_weather <- function(location) {
  list(location = location, temperature = "70 F")
}

weather_tool <- foundry_tool(
  get_weather,
  description = "Get weather for a location",
  parameters = list(
    type = "object",
    properties = list(location = list(type = "string")),
    required = "location"
  )
)

foundry_agent(
  "What is the weather in San Francisco?",
  tools = list(weather_tool)
)
}
}
\references{
\itemize{
\item Responses API function calling:
\url{https://learn.microsoft.com/azure/foundry/openai/how-to/responses#function-calling}
}
}
