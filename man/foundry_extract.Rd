% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/responses.R
\name{foundry_extract}
\alias{foundry_extract}
\title{Extract structured data from text using JSON Schema}
\usage{
foundry_extract(
  text,
  schema = NULL,
  text_col = NULL,
  instructions = NULL,
  schema_name = "ExtractedData",
  strict = TRUE,
  model = NULL,
  flatten = TRUE,
  store = FALSE,
  max_active = 2L,
  progress = TRUE,
  api_key = NULL,
  endpoint = NULL,
  ...
)
}
\arguments{
\item{text}{Character vector or data frame. Texts to extract from, or a data
frame containing a text column.}

\item{schema}{List. JSON Schema object describing the fields to extract.}

\item{text_col}{Character. Column name containing text when \code{text} is a data
frame.}

\item{instructions}{Character. Optional extraction instructions. If omitted,
a concise default extraction instruction is used.}

\item{schema_name}{Character. Name for the JSON Schema format.}

\item{strict}{Logical. Whether the model must strictly follow the schema.}

\item{model}{Character. The model deployment name. Defaults to
\code{AZURE_FOUNDRY_MODEL}.}

\item{flatten}{Logical. If \code{TRUE}, top-level schema fields are returned as
tibble columns. Nested objects and arrays become list-columns. If \code{FALSE},
parsed data is returned in a \code{.data} list-column.}

\item{store}{Logical. Whether to store Responses API objects. Defaults to
\code{FALSE} because bulk extraction often processes sensitive research data.}

\item{max_active}{Integer. Maximum number of concurrent requests.}

\item{progress}{Logical. Whether to show a progress bar for parallel
extraction.}

\item{api_key}{Character. Optional API key override.}

\item{endpoint}{Character. Optional endpoint override.}

\item{...}{Additional parameters passed to \code{foundry_response()}.}
}
\value{
A tibble with one row per input text. Metadata columns are prefixed
with \code{.}, followed by extracted schema fields when \code{flatten = TRUE}.
}
\description{
Apply a JSON Schema to one or more text inputs and return model-extracted
fields as a tidy tibble. This is useful for research coding tasks such as
sentiment annotation, entity extraction, study abstraction, and converting
free-text records into analyzable variables.
}
\examples{
\dontrun{
schema <- list(
  type = "object",
  properties = list(
    sentiment = list(type = "string", enum = c("positive", "negative", "neutral")),
    entities = list(type = "array", items = list(type = "string"))
  ),
  required = c("sentiment", "entities"),
  additionalProperties = FALSE
)

foundry_extract(
  c("I love using R with Azure.", "The workflow was slow and confusing."),
  schema = schema
)
}
}
\references{
\itemize{
\item Structured outputs:
\url{https://learn.microsoft.com/azure/foundry/openai/how-to/structured-outputs}
}
}
