% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/agents.R
\name{foundry_agent_create}
\alias{foundry_agent_create}
\title{Create a Foundry agent}
\usage{
foundry_agent_create(
  name,
  model = NULL,
  instructions = NULL,
  description = NULL,
  metadata = NULL,
  temperature = NULL,
  top_p = NULL,
  tools = NULL,
  tool_choice = NULL,
  definition = NULL,
  api_key = NULL,
  token = NULL,
  endpoint = NULL,
  api_version = "v1"
)
}
\arguments{
\item{name}{Character. Agent name. Up to 63 characters, alphanumeric and
hyphens, unique within the project.}

\item{model}{Character. Model deployment name. Required unless \code{definition}
is supplied.}

\item{instructions}{Character. Optional system prompt.}

\item{description}{Character. Optional human-readable description.}

\item{metadata}{Named list. Optional key-value metadata (up to 16 pairs).}

\item{temperature}{Numeric. Optional sampling temperature in \verb{[0, 2]}.}

\item{top_p}{Numeric. Optional nucleus-sampling value in \verb{[0, 1]}.}

\item{tools}{List. Optional tools: \code{\link[=foundry_tool]{foundry_tool()}} objects or raw tool
definition lists.}

\item{tool_choice}{Character or list. Optional tool-choice control.}

\item{definition}{List. Optional full agent definition. When supplied, the
individual \code{model}/\code{instructions}/\code{tools} arguments are ignored and this
list is sent as-is (must include \code{kind}).}

\item{api_key}{Character. Optional API key. Falls back to configured auth.}

\item{token}{Character. Optional bearer token. Falls back to configured auth.}

\item{endpoint}{Character. Optional project endpoint override.}

\item{api_version}{Character. API version query value. Defaults to \code{"v1"}.}
}
\value{
A one-row tibble describing the created agent.
}
\description{
Create a named, versioned prompt agent on the project-scoped Agent Service.
The agent bundles a model, system instructions, and optional tools so it can
later be run by name through \code{\link[=foundry_response]{foundry_response()}}.
}
\examples{
\dontrun{
foundry_agent_create(
  name = "france-facts",
  model = "gpt-4.1-mini",
  instructions = "You answer questions about France concisely."
)
}
}
