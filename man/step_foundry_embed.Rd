% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/tidymodels.R
\name{step_foundry_embed}
\alias{step_foundry_embed}
\alias{tidy.step_foundry_embed}
\title{Foundry Embedding Recipe Step}
\usage{
step_foundry_embed(
  recipe,
  ...,
  role = "predictor",
  trained = FALSE,
  model = NULL,
  dimensions = NULL,
  prefix = "emb_",
  keep_original = FALSE,
  cache = c("none", "disk"),
  cache_dir = NULL,
  columns = NULL,
  skip = FALSE,
  id = recipes::rand_id("foundry_embed")
)

\method{tidy}{step_foundry_embed}(x, ...)
}
\arguments{
\item{recipe}{A recipe object. The step will be added to the sequence of
operations for this recipe.}

\item{...}{Not used}

\item{role}{Character. Role for the new embedding variables.
Default: \code{"predictor"}.}

\item{trained}{Logical. Internal use only. Indicates whether the step has been
trained.}

\item{model}{Character. The deployment name of an Azure AI Foundry embedding
model (e.g., "text-embedding-ada-002", "text-embedding-3-small"). If \code{NULL},
defaults to the \code{AZURE_FOUNDRY_EMBED_MODEL} environment variable.}

\item{dimensions}{Integer or NULL. The number of dimensions for the output
embeddings. Only supported by some models (e.g., text-embedding-3-*).
If \code{NULL}, uses the model's default dimensionality.}

\item{prefix}{Character. Prefix for the new embedding column names.
Default: \code{"emb_"}. Columns will be named \verb{\{prefix\}\{original_col\}_\{1\}},
\verb{\{prefix\}\{original_col\}_\{2\}}, etc.}

\item{keep_original}{Logical. Should the original text column(s) be retained?
Default: \code{FALSE}.}

\item{cache}{Character. Embedding cache mode. \code{"none"} (default) always calls
the API; \code{"disk"} caches each text's embedding on disk (keyed on the text,
model, and dimensions) so cross-validation folds and repeated bakes reuse
embeddings instead of re-calling the API.}

\item{cache_dir}{Character. Directory for the disk cache. Defaults to
\code{tools::R_user_dir("foundryR", "cache")}. Clear it with
\code{\link[=foundry_cache_clear]{foundry_cache_clear()}}.}

\item{columns}{Character vector. Internal use only. Stores column names after
training.}

\item{skip}{Logical. Should the step be skipped when the recipe is baked?
While all operations are baked when \code{\link[recipes:prep]{recipes::prep()}} is run, some
operations may not be applicable to new data (e.g., processing the
outcome variable). Default: \code{FALSE}.}

\item{id}{Character. Unique identifier for this step. Automatically generated
if not provided.}

\item{x}{A \code{step_foundry_embed} object}
}
\value{
An updated recipe object with the new step appended to the sequence
of existing steps.

A tibble with columns: \code{terms}, \code{model}, \code{dimensions}, \code{id}
}
\description{
Create text embeddings using an Azure AI Foundry model as part of a tidymodels
recipe. This step converts text columns into embedding features for downstream
modeling tasks such as classification, regression, or clustering.
}
\details{
This step uses \code{\link[=foundry_embed]{foundry_embed()}} to generate embeddings for each text column
specified. During the \code{bake} phase, each text value is sent to the Azure AI
Foundry API, and the resulting embedding vector is expanded into multiple
numeric columns.
\subsection{Column naming}{

For a text column named \code{"description"} with 1536-dimensional embeddings and
the default prefix \code{"emb_"}, the output columns will be named:
\code{emb_description_1}, \code{emb_description_2}, ..., \code{emb_description_1536}.
}

\subsection{Handling failures}{

If an embedding request fails for a particular row (e.g., due to API errors),
the corresponding embedding columns will be filled with \code{NA} values for that
row.
}

\subsection{Performance considerations}{

Embedding generation requires API calls for each unique text value. For large
datasets or resampling, consider:
\itemize{
\item Setting \code{cache = "disk"} so repeated bakes and cross-validation folds reuse
embeddings instead of re-calling the API
\item Using \code{skip = TRUE} during cross-validation to avoid redundant API calls
\item Using batch processing strategies for very large datasets
}
}
}
\examples{
\dontrun{
library(recipes)

# Sample data
df <- data.frame(
  text = c("Hello world", "Machine learning is great", "R is awesome"),
  category = c("greeting", "tech", "tech")
)

# Create a recipe with Foundry embeddings
rec <- recipe(~ text, data = df) \%>\%
  step_foundry_embed(text, model = "text-embedding-ada-002")

# Prepare and bake the recipe
prepped <- prep(rec, training = df)
baked <- bake(prepped, new_data = df)

# With custom dimensions (model-dependent)
rec_custom <- recipe(~ text, data = df) \%>\%
  step_foundry_embed(
    text,
    model = "text-embedding-3-small",
    dimensions = 256,
    prefix = "vec_"
  )

# Keep original text column
rec_keep <- recipe(~ text, data = df) \%>\%
  step_foundry_embed(text, model = "text-embedding-ada-002", keep_original = TRUE)

# Use in a tidymodels workflow
library(tidymodels)

wf <- workflow() \%>\%
  add_recipe(rec) \%>\%
  add_model(logistic_reg()) \%>\%
  fit(data = train_data)
}

}
\seealso{
\code{\link[=foundry_embed]{foundry_embed()}} for the underlying embedding function,
\code{\link[recipes:recipe]{recipes::recipe()}} for creating recipes,
\code{\link[recipes:prep]{recipes::prep()}} and \code{\link[recipes:bake]{recipes::bake()}} for processing recipes.
}
\concept{preprocessing steps}
