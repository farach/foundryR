% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/evals.R
\name{foundry_eval_item}
\alias{foundry_eval_item}
\title{Build an evaluation item for model-based graders}
\usage{
foundry_eval_item(
  content,
  role = c("user", "assistant", "system", "developer")
)
}
\arguments{
\item{content}{Character. The message content. May contain \code{{{...}}}
template references.}

\item{role}{Character. One of \code{"user"}, \code{"assistant"}, \code{"system"}, or
\code{"developer"}. Defaults to \code{"user"}.}
}
\value{
A named list with \code{role} and \code{content}, ready to place in a grader
\code{input} list.
}
\description{
Model-based graders (\code{foundry_grader_label_model()} and
\code{foundry_grader_score_model()}) accept an \code{input} list of message-shaped
items. Each item has a \code{role} and \code{content}, and the content may embed
template references such as \code{{{item.question}}} or \code{{{sample.output_text}}}
that Azure resolves per row at evaluation time.
}
\examples{
foundry_eval_item("Grade this answer: {{sample.output_text}}", role = "user")
}
