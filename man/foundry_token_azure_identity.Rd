% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/token-azure-identity.R
\name{foundry_token_azure_identity}
\alias{foundry_token_azure_identity}
\title{Create a Microsoft Entra ID token provider using AzureAuth}
\usage{
foundry_token_azure_identity(
  resource = "https://cognitiveservices.azure.com",
  tenant = Sys.getenv("AZURE_TENANT_ID"),
  app = Sys.getenv("AZURE_CLIENT_ID"),
  password = NULL,
  username = NULL,
  certificate = NULL,
  auth_type = NULL,
  managed_identity = FALSE,
  version = 1,
  ...
)
}
\arguments{
\item{resource}{Character. The token audience. Defaults to
\code{"https://cognitiveservices.azure.com"} for resource-level v1 and Content
Safety APIs. Use \code{"https://ai.azure.com"} for project APIs and register the
provider with \code{scope = "project"}.}

\item{tenant}{Character. Microsoft Entra ID tenant. Defaults to the
\code{AZURE_TENANT_ID} environment variable. Ignored when
\code{managed_identity = TRUE}.}

\item{app}{Character. Application (client) ID. Defaults to the
\code{AZURE_CLIENT_ID} environment variable. Ignored when
\code{managed_identity = TRUE}.}

\item{password}{Character or \code{NULL}. Client secret for a service principal, or
the resource-owner password. \code{NULL} selects an interactive or device-code
flow.}

\item{username}{Character or \code{NULL}. Username for the resource-owner flow.}

\item{certificate}{Character or \code{NULL}. Path to, or contents of, a certificate
for certificate-based service-principal authentication.}

\item{auth_type}{Character or \code{NULL}. Explicit \pkg{AzureAuth} authentication
type. \code{NULL} lets \pkg{AzureAuth} choose based on the other arguments.}

\item{managed_identity}{Logical. If \code{TRUE}, acquire a token from an Azure
managed identity via \code{\link[AzureAuth:get_managed_token]{AzureAuth::get_managed_token()}} and ignore \code{tenant}
and \code{app}. Default \code{FALSE}.}

\item{version}{Integer. Microsoft Entra ID endpoint version, \code{1} or \code{2}.
Default \code{1}, matching the resource-style \code{resource} above.}

\item{...}{Additional arguments passed to \code{\link[AzureAuth:get_azure_token]{AzureAuth::get_azure_token()}} or
\code{\link[AzureAuth:get_managed_token]{AzureAuth::get_managed_token()}}.}
}
\value{
A zero-argument token provider function suitable for
\code{\link[=foundry_set_token_provider]{foundry_set_token_provider()}}.
}
\description{
Create a provider function for \code{\link[=foundry_set_token_provider]{foundry_set_token_provider()}} that acquires
Microsoft Entra ID access tokens through the \pkg{AzureAuth} package. This
supports service principals (client secret or certificate), managed identity,
and interactive or device-code flows, and refreshes tokens automatically as
they approach expiry.
}
\details{
The token is acquired lazily on first use, so building the provider never
triggers a network call. Tokens are re-acquired within five minutes of
expiry; \pkg{AzureAuth} reuses its on-disk cache and refresh tokens under the
hood, so re-acquisition is inexpensive and does not re-prompt for interactive
flows.
}
\examples{
\dontrun{
# Service principal with a client secret
foundry_set_token_provider(
  foundry_token_azure_identity(
    tenant = "your-tenant-id",
    app = "your-client-id",
    password = Sys.getenv("AZURE_CLIENT_SECRET")
  )
)

# Managed identity inside Azure
foundry_set_token_provider(
  foundry_token_azure_identity(managed_identity = TRUE)
)

# Project-scoped APIs use a different audience and provider slot
foundry_set_token_provider(
  foundry_token_azure_identity(
    resource = "https://ai.azure.com",
    managed_identity = TRUE
  ),
  scope = "project"
)
}
}
\seealso{
\code{\link[=foundry_token_azure_cli]{foundry_token_azure_cli()}} for a provider that shells out to the
Azure CLI instead.
}
