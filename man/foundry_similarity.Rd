% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/embed.R
\name{foundry_similarity}
\alias{foundry_similarity}
\title{Compute Cosine Similarity Between Embeddings}
\usage{
foundry_similarity(data, text_col = "text", top_k = NULL, as_matrix = FALSE)
}
\arguments{
\item{data}{A tibble from \code{foundry_embed()} containing an \code{embedding} list-column.}

\item{text_col}{Character. Name of the column containing text labels. Default: "text".}

\item{top_k}{Integer. Optional maximum number of most-similar pairs to return.}

\item{as_matrix}{Logical. If \code{TRUE}, return the full cosine-similarity matrix
instead of a long pairwise tibble.}
}
\value{
If \code{as_matrix = FALSE}, a tibble with columns:
\describe{
\item{text_1}{Character. First text.}
\item{text_2}{Character. Second text.}
\item{similarity}{Numeric. Cosine similarity between -1 and 1.}
}
If \code{as_matrix = TRUE}, a numeric cosine-similarity matrix with row and
column names from \code{text_col}.
}
\description{
Compute pairwise cosine similarity between all embeddings in a tibble.
Useful for finding semantically similar texts.
}
\examples{
# Toy vectors demonstrate local computation without calling Azure.
embeddings <- tibble::tibble(
  text = c("Vector A", "Vector B", "Vector C"),
  embedding = list(c(1, 0), c(1, 1), c(0, 1))
)
foundry_similarity(embeddings)
foundry_similarity(embeddings, top_k = 1)
foundry_similarity(embeddings, as_matrix = TRUE)
}
