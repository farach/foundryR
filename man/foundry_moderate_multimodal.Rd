% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/content-safety-preview.R
\name{foundry_moderate_multimodal}
\alias{foundry_moderate_multimodal}
\title{Moderate an image together with its text}
\usage{
foundry_moderate_multimodal(
  image,
  text = NULL,
  categories = c("Hate", "Sexual", "SelfHarm", "Violence"),
  enable_ocr = TRUE,
  endpoint = NULL,
  api_key = NULL,
  api_version = "2024-09-15-preview"
)
}
\arguments{
\item{image}{Character. Local image path or HTTPS Azure Blob Storage URL.}

\item{text}{Character. Optional text shown with the image (max 1,000 code
points).}

\item{categories}{Character vector of harm categories. Defaults to all four.}

\item{enable_ocr}{Logical. When \code{TRUE}, run OCR on the image to recognize
embedded text. Default \code{TRUE}.}

\item{endpoint}{Character. Optional Content Safety endpoint.}

\item{api_key}{Character. Optional Content Safety key.}

\item{api_version}{Character. API version. Defaults to
\code{"2024-09-15-preview"}.}
}
\value{
A tibble with one row per harm category, matching
\code{\link[=foundry_moderate_image]{foundry_moderate_image()}}: \code{source}, \code{category}, \code{severity}, \code{label}, and
\code{raw_response}. Multimodal analysis returns four-level severities
(0, 2, 4, 6).
}
\description{
Analyze an image and optional accompanying text in a single multimodal
Content Safety call. Optical character recognition can read text embedded in
the image so that harmful captions or overlays are caught alongside the
picture.
}
\section{Preview API}{

This operation is documented only in the Azure AI Content Safety Learn
quickstarts and has no published OpenAPI specification. It requires the
\code{2024-09-15-preview} api-version and, at time of writing, is available only
in a subset of Azure regions.
}

\examples{
\dontrun{
foundry_moderate_multimodal(
  image = "meme.png",
  text = "caption under the image",
  enable_ocr = TRUE
)
}
}
