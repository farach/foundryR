% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/evals.R
\name{foundry_grader_azure_ai}
\alias{foundry_grader_azure_ai}
\title{Azure AI built-in evaluator grader}
\usage{
foundry_grader_azure_ai(
  name,
  evaluator_name,
  initialization_parameters = NULL,
  data_mapping = NULL,
  evaluator_version = NULL
)
}
\arguments{
\item{name}{Character. Grader name shown in results.}

\item{evaluator_name}{Character. The evaluator ID, e.g. \code{"builtin.coherence"}.}

\item{initialization_parameters}{List. Optional parameters passed to the
evaluator, e.g. \code{list(model = "gpt-4.1-mini")} for model-graded evaluators.}

\item{data_mapping}{Named list. Optional mapping from evaluator inputs to
dataset templates, e.g. \code{list(query = "{{item.query}}", response = "{{sample.output_text}}")}.}

\item{evaluator_version}{Character. Optional evaluator version. Defaults to
the latest version on the service when omitted.}
}
\value{
A named list describing an \code{azure_ai_evaluator} grader.
}
\description{
Reference an Azure AI Foundry built-in evaluator (a \verb{builtin.*} ID such as
\code{builtin.coherence} or \code{builtin.groundedness}) as a grader. This grader type
is only available on the project-scoped Foundry endpoint.
}
\examples{
foundry_grader_azure_ai(
  name = "coherence",
  evaluator_name = "builtin.coherence",
  initialization_parameters = list(model = "gpt-4.1-mini"),
  data_mapping = list(
    query = "{{item.query}}",
    response = "{{sample.output_text}}"
  )
)
}
