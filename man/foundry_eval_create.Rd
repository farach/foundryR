% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/evals.R
\name{foundry_eval_create}
\alias{foundry_eval_create}
\title{Create an evaluation}
\usage{
foundry_eval_create(
  name = NULL,
  data_source_config,
  testing_criteria,
  metadata = NULL,
  api_key = NULL,
  token = NULL,
  endpoint = NULL,
  api_version = NULL
)
}
\arguments{
\item{name}{Character. Optional evaluation name.}

\item{data_source_config}{List. A configuration from
\code{\link[=foundry_eval_data_config]{foundry_eval_data_config()}}.}

\item{testing_criteria}{List. A grader from \verb{foundry_grader_*()}, or a list of
graders.}

\item{metadata}{List. Optional metadata attached to the evaluation.}

\item{api_key}{Character. Optional API key. Falls back to configured auth.}

\item{token}{Character. Optional bearer token. Falls back to configured auth.}

\item{endpoint}{Character. Optional endpoint override.}

\item{api_version}{Character. Optional \code{api-version} query value. The Foundry
v1 evals surface is path-versioned, so this is usually left \code{NULL}.}
}
\value{
A one-row tibble describing the created evaluation.
}
\description{
Create an evaluation group that pairs a data-source configuration with one or
more graders (\code{testing_criteria}). Evaluations are run against data with
\code{\link[=foundry_eval_run_create]{foundry_eval_run_create()}}.
}
\examples{
\dontrun{
foundry_eval_create(
  name = "qa-accuracy",
  data_source_config = foundry_eval_data_config(
    type = "custom",
    item_schema = list(
      type = "object",
      properties = list(answer = list(type = "string")),
      required = list("answer")
    ),
    include_sample_schema = TRUE
  ),
  testing_criteria = foundry_grader_string_check(
    name = "exact",
    input = "{{sample.output_text}}",
    reference = "{{item.answer}}",
    operation = "eq"
  )
)
}
}
