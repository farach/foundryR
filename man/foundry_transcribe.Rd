% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/audio.R
\name{foundry_transcribe}
\alias{foundry_transcribe}
\title{Transcribe an audio file with Microsoft Foundry}
\usage{
foundry_transcribe(
  file,
  model = NULL,
  service = c("speech", "openai"),
  api = c("v1", "deployment"),
  locales = NULL,
  language = NULL,
  prompt = NULL,
  transcribe_style = NULL,
  phrase_list = NULL,
  response_format = NULL,
  timestamp_granularities = NULL,
  include = NULL,
  temperature = NULL,
  api_key = NULL,
  token = NULL,
  endpoint = NULL,
  api_version = NULL
)
}
\arguments{
\item{file}{Character. Local audio file path.}

\item{model}{Character. Model or deployment name. Defaults to
\code{"mai-transcribe-1.5"} for \code{service = "speech"} and to
\code{AZURE_FOUNDRY_MODEL} for \code{service = "openai"}.}

\item{service}{Character. \code{"speech"} for LLM Speech/MAI-Transcribe or
\code{"openai"} for \verb{/openai/v1/audio/transcriptions}.}

\item{api}{Character. Used when \code{service = "openai"}. \code{"v1"} calls the
\verb{/openai/v1/...} data-plane path; \code{"deployment"} calls
\verb{/openai/deployments/\{model\}/...}. Classic \code{whisper} deployments require
\code{"deployment"}; \verb{gpt-4o-transcribe}-family models use \code{"v1"}.}

\item{locales}{Character vector. Optional Speech locale hints such as
\code{"en-US"} or \code{"es-ES"}.}

\item{language}{Character. Optional OpenAI transcription language hint such as
\code{"en"} or \code{"es"}.}

\item{prompt}{Character vector. Optional prompt instructions.}

\item{transcribe_style}{Character. Optional MAI-Transcribe 1.5 style, such as
\code{"verbatim"}.}

\item{phrase_list}{Character vector. Optional phrases for MAI-Transcribe 1.5.}

\item{response_format}{Character. Optional OpenAI response format.}

\item{timestamp_granularities}{Character vector. Optional OpenAI timestamp
granularities, such as \code{"segment"} or \code{"word"}.}

\item{include}{Character vector. Optional OpenAI include values.}

\item{temperature}{Numeric. Optional OpenAI sampling temperature.}

\item{api_key}{Character. Optional API key override.}

\item{token}{Character. Optional bearer token override.}

\item{endpoint}{Character. Optional endpoint override.}

\item{api_version}{Character. Optional API version. Defaults to
\code{"2025-10-15"} for Speech and \code{"preview"} for OpenAI audio.}
}
\value{
A one-row tibble with transcript text, phrase-level detail, and the
raw response in list-columns.
}
\description{
Transcribe audio through the Speech in Foundry Tools LLM Speech API, including
MAI-Transcribe models, or through the Azure OpenAI v1 preview audio endpoint.
}
\examples{
\dontrun{
foundry_transcribe("interview.mp3", model = "mai-transcribe-1.5")
foundry_transcribe("interview.mp3", service = "openai", model = "gpt-4o-transcribe")
foundry_transcribe("speech.wav", service = "openai", model = "whisper", api = "deployment")
}
}
