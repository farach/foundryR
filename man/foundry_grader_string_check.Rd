% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/evals.R
\name{foundry_grader_string_check}
\alias{foundry_grader_string_check}
\title{String-check grader}
\usage{
foundry_grader_string_check(
  name,
  input,
  reference,
  operation = c("eq", "ne", "like", "ilike")
)
}
\arguments{
\item{name}{Character. Grader name shown in results.}

\item{input}{Character. Input text, typically a template such as
\code{"{{sample.output_text}}"}.}

\item{reference}{Character. Reference text, typically a template such as
\code{"{{item.expected}}"}.}

\item{operation}{Character. One of \code{"eq"}, \code{"ne"}, \code{"like"}, or \code{"ilike"}.}
}
\value{
A named list describing a \code{string_check} grader, for use in the
\code{testing_criteria} of \code{\link[=foundry_eval_create]{foundry_eval_create()}}.
}
\description{
Compare a templated input string against a reference string with an exact or
pattern operation. Useful for deterministic pass/fail checks such as verifying
an extracted field matches a known value.
}
\examples{
foundry_grader_string_check(
  name = "exact-match",
  input = "{{sample.output_text}}",
  reference = "{{item.answer}}",
  operation = "eq"
)
}
