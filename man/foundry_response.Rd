% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/responses.R
\name{foundry_response}
\alias{foundry_response}
\title{Create a response with the Azure OpenAI Responses API}
\usage{
foundry_response(
  input,
  model = NULL,
  instructions = NULL,
  previous_response_id = NULL,
  tools = NULL,
  text_format = NULL,
  max_output_tokens = NULL,
  temperature = NULL,
  top_p = NULL,
  reasoning_effort = NULL,
  reasoning_summary = NULL,
  store = NULL,
  background = NULL,
  conversation = NULL,
  prompt_cache_key = NULL,
  prompt_cache_retention = NULL,
  parallel_tool_calls = NULL,
  max_tool_calls = NULL,
  safety_identifier = NULL,
  metadata = NULL,
  include = NULL,
  parse_json = !is.null(text_format),
  api_key = NULL,
  endpoint = NULL,
  agent = NULL,
  agent_version = NULL,
  ...
)
}
\arguments{
\item{input}{Character scalar or list. The user input for the response. A
character scalar is sent directly. A list can contain Responses API input
items for advanced use cases.}

\item{model}{Character. The model deployment name. Defaults to the
\code{AZURE_FOUNDRY_MODEL} environment variable.}

\item{instructions}{Character. Optional system/developer instructions.}

\item{previous_response_id}{Character. Optional response ID to continue a
stored conversation.}

\item{tools}{List. Optional Responses API tools, for example
\code{list(list(type = "web_search"))} or a list of \code{\link[=foundry_tool]{foundry_tool()}} objects.}

\item{text_format}{List. Optional Responses API text format object. Use
\code{list(type = "json_object")} for JSON mode or
\code{list(type = "json_schema", name = ..., schema = ..., strict = TRUE)} for
structured outputs.}

\item{max_output_tokens}{Integer. Optional maximum generated output tokens.}

\item{temperature}{Numeric. Optional sampling temperature. Do not use with
reasoning-only models that reject sampling parameters.}

\item{top_p}{Numeric. Optional nucleus sampling parameter. Do not use with
reasoning-only models that reject sampling parameters.}

\item{reasoning_effort}{Character. Optional reasoning effort (\code{"low"},
\code{"medium"}, \code{"high"}, or a newer value supported by your model). Sent as
\code{reasoning = list(effort = ...)}.}

\item{reasoning_summary}{Character. Optional reasoning summary mode for
models that support it.}

\item{store}{Logical or NULL. Whether the service should store the response.
The API stores responses by default when this is omitted. Set \code{FALSE} for
stateless calls; use \code{TRUE} or omit it when chaining with
\code{previous_response_id}.}

\item{background}{Logical. Whether to run the response in the background.}

\item{conversation}{Character. Optional conversation ID for server-side
conversation state.}

\item{prompt_cache_key, prompt_cache_retention}{Optional prompt-cache controls.}

\item{parallel_tool_calls}{Logical. Whether the service may call tools in
parallel.}

\item{max_tool_calls}{Integer. Optional maximum number of tool calls.}

\item{safety_identifier}{Character. Optional stable end-user identifier for
safety monitoring.}

\item{metadata}{List. Optional metadata to attach to the response.}

\item{include}{Character vector. Optional additional response fields to
include.}

\item{parse_json}{Logical. Whether to parse \code{output_text} as JSON into the
\code{structured} list-column. Defaults to \code{TRUE} when \code{text_format} is supplied.}

\item{api_key}{Character. Optional API key override.}

\item{endpoint}{Character. Optional endpoint override. When \code{agent} is
supplied this is treated as the project endpoint override.}

\item{agent}{Character or list. Optional agent to run instead of a bare
model: an agent name, a \code{\link[=foundry_agent_reference]{foundry_agent_reference()}} object, or a one-row
tibble from \code{\link[=foundry_agent_create]{foundry_agent_create()}}. When supplied, \code{model} is ignored,
\code{agent_reference} is sent in the request body, and the call is routed to
the project-scoped endpoint.}

\item{agent_version}{Character. Optional agent version to pin when \code{agent} is
a bare name. Omit to use the latest version.}

\item{...}{Additional request body parameters passed to the Responses API.}
}
\value{
A one-row tibble with response metadata, generated text, parsed
structured output (if requested), citations, tool calls, token usage, and
the raw response as a list-column.
}
\description{
Use Microsoft Foundry's newer \verb{/openai/v1/responses} API to generate model
responses, chain stateful turns with \code{previous_response_id}, call built-in
tools such as web search, and request schema-constrained structured output.
}
\details{
The Responses API uses the v1 endpoint style:
\verb{https://<resource>.openai.azure.com/openai/v1/responses}. Unlike the older
chat-completions API, the model deployment is supplied in the JSON body as
\code{model}.

\strong{Stored responses and privacy:} Microsoft Foundry stores Responses API
objects by default. Set \code{store = FALSE} for stateless calls when you do not
need server-side conversation state. To use \code{previous_response_id} chaining,
the previous response must have been stored.
}
\examples{
\dontrun{
foundry_response("Summarize retrieval-augmented generation.")

first <- foundry_response("Define catastrophic forgetting.")
foundry_response(
  "Explain it for a college freshman.",
  previous_response_id = first$response_id
)
}
}
\references{
\itemize{
\item Azure OpenAI Responses API:
\url{https://learn.microsoft.com/azure/foundry/openai/how-to/responses}
\item Azure OpenAI REST API reference:
\url{https://learn.microsoft.com/azure/foundry/openai/reference}
}
}
