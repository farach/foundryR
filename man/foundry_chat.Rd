% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/chat.R
\name{foundry_chat}
\alias{foundry_chat}
\title{Chat with an Azure AI Model}
\usage{
foundry_chat(
  message,
  system = NULL,
  model = NULL,
  history = NULL,
  temperature = NULL,
  max_tokens = NULL,
  max_completion_tokens = NULL,
  top_p = NULL,
  frequency_penalty = NULL,
  presence_penalty = NULL,
  stop = NULL,
  api_key = NULL,
  api_version = NULL,
  ...
)
}
\arguments{
\item{message}{Character. The user message to send.}

\item{system}{Character. Optional system prompt to set the assistant's behavior.}

\item{model}{Character. The deployment name. Defaults to the environment variable
\code{AZURE_FOUNDRY_MODEL} or must be specified.}

\item{history}{List. Optional conversation history as a list of message objects,
each with \code{role} and \code{content} fields.}

\item{temperature}{Numeric. Sampling temperature between 0 and 2. Higher values
make output more random, lower values more deterministic. Default: 1.}

\item{max_tokens}{Integer. Maximum tokens in response (legacy parameter, use
\code{max_completion_tokens} for newer models).}

\item{max_completion_tokens}{Integer. Maximum tokens in response. Preferred
parameter for newer models (gpt-4o, etc.). Takes precedence over \code{max_tokens}.}

\item{top_p}{Numeric. Nucleus sampling parameter between 0 and 1. Default: 1.}

\item{frequency_penalty}{Numeric. Penalty for token frequency (-2.0 to 2.0). Default: 0.}

\item{presence_penalty}{Numeric. Penalty for token presence (-2.0 to 2.0). Default: 0.}

\item{stop}{Character vector. Up to 4 sequences where the API will stop generating.}

\item{api_key}{Character. Optional API key override.}

\item{api_version}{Character. Optional API version override.}

\item{...}{Additional parameters passed to the API.}
}
\value{
A tibble with columns:
\describe{
\item{role}{Character. Always "assistant".}
\item{content}{Character. The generated response text.}
\item{model}{Character. The deployment/model name used.}
\item{finish_reason}{Character. Why generation stopped: "stop", "length", etc.}
\item{prompt_tokens}{Integer. Tokens in the prompt.}
\item{completion_tokens}{Integer. Tokens in the response.}
\item{total_tokens}{Integer. Total tokens used.}
}
}
\description{
Send a message to an Azure AI Foundry deployed model and receive a response.
Returns a tibble with the assistant's response and usage metadata.
}
\examples{
\dontrun{
# Simple chat
foundry_chat("What is the capital of France?", model = "gpt-4")

# With system prompt
foundry_chat(
  "Explain tibbles",
  system = "You are a helpful R programming tutor. Be concise.",
  model = "gpt-4"
)

# With parameters (use max_completion_tokens for newer models)
foundry_chat(
  "Write a haiku about data science",
  model = "gpt-4",
  temperature = 0.9,
  max_completion_tokens = 100
)

# With conversation history
history <- list(
  list(role = "user", content = "My name is Alex"),
  list(role = "assistant", content = "Hello Alex! How can I help you?")
)
foundry_chat("What's my name?", model = "gpt-4", history = history)
}
}
